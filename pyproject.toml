[project]
name = "fast-weights-attention"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "torchvision>=0.28.0",
    "transformers>=5.16.1",
]
